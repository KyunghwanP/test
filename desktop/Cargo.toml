[package]
name = "ynhs-desktop"
version = "0.1.0"
edition = "2021"

[dependencies]
wry = { version = "0.35", features = ["rwh_06"] }
tao = "0.24"
webbrowser = "0.8"

[profile.release]
opt-level = "z"
lto = true
strip = true
panic = "abort"
